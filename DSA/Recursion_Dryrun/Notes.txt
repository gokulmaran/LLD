Recursion dry run 

1) dry run-->Decision tree of each call return
2) logic-->
linear (single rec call), 
tree rec(each index (>1)rec call works from last)
for loop (explore all possibilities, from 1st case )